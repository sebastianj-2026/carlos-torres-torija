# Backlog — Carlos Torres Torija

## Reglas de tamaño

Una tarea es atómica si cumple las 5:

1. Toca **un** módulo
2. Toca **≤5** archivos
3. **Pasa el gate por sí sola** — no "queda verde cuando termine la siguiente"
4. Cabe en un commit con mensaje de una línea
5. Si el título necesita "y", son dos tareas

Si no cumple → se parte. No se negocia.

## Estados
`⬜ pendiente` · `🟡 en curso` · `✅ hecha` · `🚫 bloqueada (regla sin definir)`

> Backlog heredado del pre-metodología: `docs/BACKLOG-referenciadores.md`.
> Migrar sus items a este formato atómico cuando se retomen. Los módulos ya
> especificados (`personas`, `comisiones`) se derivan a tareas con
> `/metodologia:derivar-backlog`.

---

### T-001 · {{TODO: primera tarea atómica — derivar de docs/modulos/comisiones}}
- **Módulo:** comisiones
- **Tipo:** motor
- **Depende de:** —
- **Lee:** `docs/modulos/comisiones/REGLAS.md` + `CASOS-RESUELTOS.md`
- **Extra al DoD:** {{TODO}}
- **Estado:** ⬜

> Solo se escribe el criterio **extra**. Los del gate se dan por hecho —
> repetirlos en cada tarea es ruido que Claude Code termina ignorando.
